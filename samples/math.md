# math

Inline math like $e^{i\pi} + 1 = 0$ flows with the text.

Display math gets its own stage:

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

$$
\begin{pmatrix} a & b \\ c & d \end{pmatrix}
\begin{pmatrix} x \\ y \end{pmatrix} =
\begin{pmatrix} ax + by \\ cx + dy \end{pmatrix}
$$

The quadratic formula, because tradition demands it:

$$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$$

Mixed with regular markdown — **bold**, `code`, and a table:

| symbol | meaning |
|---|---|
| $\nabla$ | gradient |
| $\partial$ | partial derivative |
| $\aleph_0$ | countable infinity |
